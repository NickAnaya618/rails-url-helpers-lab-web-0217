<h1><%= link_to @student.to_s, students_path(@student) %></h1>

<% if @student.active == false %>
  <%= "This student is currently inactive." %>
<% else %>
  <%= "This student is currently active." %>
<% end %>
